# ============================================================
# localhost
# ============================================================

<VirtualHost *:80>

    ServerName localhost

    DocumentRoot "/var/www/sites/main"

    <Directory "/var/www/sites/main">
        Options FollowSymLinks
        AllowOverride All
        Require all granted
    </Directory>

    DirectoryIndex index.php index.html

    <FilesMatch "\.php$">
        SetHandler "proxy:fcgi://php-main:9000"
    </FilesMatch>

    ErrorLog /proc/self/fd/2
    CustomLog /proc/self/fd/1 combined

</VirtualHost>


# ============================================================
# app1.localhost
# ============================================================

<VirtualHost *:80>

    ServerName app1.localhost

    DocumentRoot "/var/www/sites/app1"

    <Directory "/var/www/sites/app1">
        Options FollowSymLinks
        AllowOverride All
        Require all granted
    </Directory>

    DirectoryIndex index.php index.html

    <FilesMatch "\.php$">
        SetHandler "proxy:fcgi://php-app1:9000"
    </FilesMatch>

    ErrorLog /proc/self/fd/2
    CustomLog /proc/self/fd/1 combined

</VirtualHost>


# ============================================================
# app2.localhost
# ============================================================

<VirtualHost *:80>

    ServerName app2.localhost

    DocumentRoot "/var/www/sites/app2"

    <Directory "/var/www/sites/app2">
        Options FollowSymLinks
        AllowOverride All
        Require all granted
    </Directory>

    DirectoryIndex index.php index.html

    <FilesMatch "\.php$">
        SetHandler "proxy:fcgi://php-app2:9000"
    </FilesMatch>

    ErrorLog /proc/self/fd/2
    CustomLog /proc/self/fd/1 combined

</VirtualHost>


# ============================================================
# shop.localhost
# ============================================================

<VirtualHost *:80>

    ServerName shop.localhost

    DocumentRoot "/var/www/sites/shop"

    <Directory "/var/www/sites/shop">
        Options FollowSymLinks
        AllowOverride All
        Require all granted
    </Directory>

    DirectoryIndex index.php index.html

    <FilesMatch "\.php$">
        SetHandler "proxy:fcgi://php-shop:9000"
    </FilesMatch>

    ErrorLog /proc/self/fd/2
    CustomLog /proc/self/fd/1 combined

</VirtualHost>